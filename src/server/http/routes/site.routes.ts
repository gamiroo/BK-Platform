// src/server/http/routes/site.routes.ts
// Placeholder route module.
// This exists so the BalanceGuard compliance gate has a stable target folder.
// Replace with real routing + BalanceGuard-wrapped handlers as you implement routes.

export function registerSiteRoutes(): void {
  // TODO: when router is introduced, register site routes here.
  // Example shape (future):
  // router.get("/health", balanceguardSite(async (ctx, req) => json(ctx, { ok: true })));
}

